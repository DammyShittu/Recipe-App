<div class="container">
<h1 class="mt-3 mb-2 text-center">Shopping List</h1>
<div class="d-flex justify-content-around mt-5 mb-5">
  <h2>Amount of food Items to buy: <%= @foods.size %> </h2>
  <h2>Total value of food needed: $<%= @total %></h2>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th scope="col">Food</th>
      <th scope="col">Quantity</th>
      <th scope="col"> Price</th>
    </tr>
  </thead>
  <tbody>
    <% @foods.each do | food | %>
    <% qty=RecipeFood.joins(:food).where(food_id: food.id).sum( 'quantity') %>
    <tr>
      <th scope="row">
        <%= food.name %>
      </th>
      <td> <%= qty %> <%= food.measurement_unit %> </td>
      <td> $ <%= food.price %></td>
    </tr>
    <% end %>
  </tbody>
</table>
</div>